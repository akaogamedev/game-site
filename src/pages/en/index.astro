---
import { getCollection } from "astro:content";
import LocalizedHome from "../../components/LocalizedHome.astro";

const games = await getCollection("gamesEn");
---

<LocalizedHome locale="en" games={games} />
