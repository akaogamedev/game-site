---
import { getCollection } from "astro:content";
import LocalizedHome from "../components/LocalizedHome.astro";

const games = await getCollection("games");
---

<LocalizedHome locale="ja" games={games} />
